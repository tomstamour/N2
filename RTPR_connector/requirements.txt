# RTPR_connector.py runtime dependencies
# Versions below are those verified working on the system python3 (3.12.3).
websockets>=15.0
aiohttp>=3.13
pandas>=2.3
lxml>=6.0
uvloop>=0.21   # optional (Linux only); the script degrades gracefully to the default loop without it
