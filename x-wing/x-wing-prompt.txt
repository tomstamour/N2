I want you to design a new script named "X-wing-1.0.py" that will place trade on a ticker using the ibapi (IBKR). The script will have to never use TickByTick data to avoid connection client issues, since TickByTick data request allow maximum 3 stock data request/scrips instances simultanously. The x-wing script will have to be able to run on at least 10 stocks simultanously. All orders placed in this script will have to be REPLACED and not CANCELED to avoid latency between order changes. The script will take as inputs:

--symbol <string>
--log-dir <path>
--limit-entry-prc <float>
--input-limits-table <path/file>
--Entry-limit-price <float>
--cash-involved <float>

This script will perform the following steps (bullets):
1- place a Buy Limit Order (Long side) at the --Entry-limit-price value

2- When the Order is fully filled or partially filled, instantly place a Sell Stop Limit for the number of shares that are currently filled. To set the Sell Stop Limit the script see the next bullets. Follow at every second the number of filled shares (long) and keep track of the "average fill price" for the position. 

3- Calculate the Current-Yield (%) of the current position, to do so , fetch reqMktData with snapshot=True and calculate the BID / ASK midpoint to determine the "current price", at a 1 second interval. Use the "average fill price" and the "current price" to calculate the Current-Yield (%) for the position.

4- Simultaneously with bullet -3 ; Place a Stop Limit Order, the --input-limits-table file will be of the following format PATH, this file will dictate the auxPrice and lmtPrice to place and order of the type Stop Limit. You will have to compare the Current-Yield value to the column "Yield (%)" of the --input-limits-table to get the current auxPrice and lmtPrice to set/adjust (never cancel,always replace the order already in place to save replacement time) the active order depending on the Current-Yield value.

5- If the price drops below the Stop Limit Order and the order is not filled, leave the order at this price and don't end the script.

6- If the Sell Stop Limit Order is filled, place a new Buy Limit Order at the same price at which the Sell Stop Limit was filled. If the price goes up again and fills the 2e Buy Limit Order, then the script must continue placing/adjusting the Sell Stop Limit order again using the same --input-limits-table file and original "average fill price".

These orders have to be valid either during ETH and RTH. I want you to think of how many ibapi clients a single x-wing instance should use to allow proper monitoring of all usefull/necessary data for the tasks to perform in this script. Also make sure all these clients will be disconnected properly when the script ends. Increment the ClientIDs starting from 10000, then 10001, 10002 ... Have a log that is visually clear for a human to follow the price action and the Stop Limits in place. Also output/create a table that will gather all price actions and the time points (use and option name --price-action-table <path/name> with name = x-wing-table-ticker-MM-HH-DD-YYYY.tsv). Use the AskUserQuestion tool to thouroughly challenge me regarding the points I might have ommited regarding this script.
